name: Database backup

on:
  schedule:
    # 03:00 UTC nightly. The free-tier Postgres host this project uses today
    # (Supabase, free plan) does not include automatic backups — see
    # docs/operational-runbook.md — so this workflow is the backup until/
    # unless that changes.
    - cron: '0 3 * * *'
  workflow_dispatch:

jobs:
  backup:
    name: pg_dump and upload
    runs-on: ubuntu-latest
    steps:
      # Dump via the official postgres:17 image rather than apt's
      # postgresql-client (Ubuntu's noble repo only has v16) -- pg_dump
      # refuses to run against a server on a newer major version than
      # itself, and Supabase runs Postgres 17.
      - name: Dump database
        run: |
          mkdir -p backup
          docker run --rm -v "$PWD/backup:/backup" postgres:17 \
            pg_dump "$DATABASE_URL" --format=custom --no-owner --no-privileges \
            --file="/backup/linkedout-$(date -u +%Y%m%dT%H%M%SZ).dump"
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}

      - name: Upload backup artifact
        uses: actions/upload-artifact@v4
        with:
          name: db-backup-${{ github.run_id }}
          path: backup/*.dump
          # Free tier's usable window — raise this only alongside an actual
          # storage plan; artifacts count against the account's storage quota.
          retention-days: 30
